{
  "name": "idea",
  "version": "0.0.0",
  "workspaces":["backend","frontend"],
  "scripts": {
   "start": "bun run backend:start && bun run frontend:start", 
   "build": "bun run backend:build && bun run frontend:build",
   "clean": "rm -rf node_modules && cd backend && rm -rf node_modules && cd ../frontend && rm -rf node_modules"
  }

}
